# 汴梁归途 Demo 验证报告

验证对象：本机静态构建版，规则版本1.0。最终核验日期：2026-09-09。

## 实际完成的检查

| 检查 | 结果 |
|---|---|
| TypeScript 类型检查 | 通过 |
| 游戏源码、测试和服务脚本的 Oxlint 检查 | 通过 |
| Node 规则与接口测试 | 18项通过，0失败 |
| 正式构建与静态预渲染 | 通过，退出码0 |
| 本机启动器 | 成功启动；重复启动复用已有服务 |
| 正式版浏览器完整流程 | 通过，第26日归航，无页面脚本错误 |
| 窄屏 | 390×844视口，完整流程无页面水平溢出 |
| 事件内容 | 8个家族、24个情境、50个选项、76个结果分支 |

规则测试覆盖现金与库存边界、无效操作原子性、价差、母鸡成本、混合来源鸡蛋成本、夜间共用粮食校验、连续断粮、保质期、三种结局、刷新恢复、事件冷却、情境排重、线索、后续来源与加权抽样。

每个已编写选项分别使用100个种子执行，所有76个结果分支均可达；每次执行均核对确定费用、行动点、健康损失边界和存档重放的一致性。

浏览器实际点击流程：开局 → 茶馆 → 买20份粟米和一只鸡 → 食宿喂鸡 → 第2日现金216文 → 刷新续玩 → 取消重开 → 查问遭遇并刷新保留线索 → 劳动与租房 → 归航。另在独立测试存储中写入损坏存档，确认页面提示且不静默覆盖。

证据见 `tests/browser-output/result.json`、`market-desktop.png`、`mobile.png`、`encounter.png` 和 `ending.png`。

## 400局基准模拟

每种策略100个种子。策略只使用当前报价、已知基础价格及玩家持有资产，不读取隐藏事实或未来事件；遇到个人遭遇默认告辞，达标前会出售货物和母鸡。

| 策略 | 局数 | 归航 | 死亡 | 平均结束日 | 平均归航前现金 |
|---|---:|---:|---:|---:|---:|
| 劳动与租房 | 100 | 100 | 0 | 20.0 | 3020文 |
| 交易与劳动 | 100 | 100 | 0 | 19.6 | 3192文 |
| 养鸡与劳动 | 100 | 100 | 0 | 22.3 | 3088文 |
| 混合经营与打听消息 | 100 | 100 | 0 | 24.1 | 3182文 |

全部模拟中未出现负现金、负库存、超容量或超过500次操作仍无法结束的状态。逐局数据保存在 `tests/simulation-results.json`。

这组结果说明谨慎路线可以达成目标，不等于普通玩家100%获胜，也不证明各路线强度完全相同。模拟采用简单启发式策略，不是最优策略搜索。

## 根据检查修复的实际问题

- 统一夜间粮食预检，避免晚饭和喂鸡重复使用同一份库存。
- 保存母鸡购入成本；混合来源鸡蛋销售分别计算自产收入和普通贸易利润。
- 事件中低价购入的货物记录净购入成本，退回零头也反映到批次成本。
- 茶馆已获得的交叉线索持久保存，不随“异乡人”效果到期消失。
- 玩家操作携带状态版本，旧页面回调和重复提交不能再次结算。
- 存档深层结构校验，损坏事件、报价、货物不会带入游戏。
- 达到归航现金门槛后，在市场或夜间同样可归航。
- 重活由90文调整为75文，减少纯劳动过早结束一局的问题。
- Windows构建成功后延迟1秒退出，等待原生关闭回调；失败退出码仍原样传递。

## 尚未验证或不能据此推断的部分

- **30～45分钟的真人游玩时长尚未验证。** 浏览器完整通关是自动点击执行，不能替代真人体验测试。
- 原生 WebMCP 在测试 Chrome 中不可用。两个可选接口已通过模拟注册与行为测试，未声称真实原生注册通过；普通页面操作不受影响。
- 事件文字已检查语境与确定数值，但内容有限，多局后仍会出现玩家熟悉的情境类型。
- 未进行公开部署、多人并发、云存档或联网安全验收；这些不属于本机 Demo 范围。

## 复现

在项目目录使用 README 所述 Node 环境运行：

```text
npm run typecheck
npm run lint
npm test
npm run simulate
npm run build
npm start
```

浏览器检查：配置 `PLAYWRIGHT_MODULE` 指向可用的 `playwright-core` 包，然后运行 `node tests/browser.mjs`。它使用独立无痕上下文，不读取日常浏览器资料。
